package Controller;

public class Calculator {
    public double getPriceProduct(int id, double weight) {


/*
  String query= "SELECT id, email, password, name, role, phone, height, weight"+" FROM Users "+
                " WHERE users.email='"+email+"' AND password='"+password+"'";

        try (Connection connection = dataBaseHandler.getDbConnection();
             Statement statement = connection.createStatement();
             ResultSet resultSet = statement.executeQuery(query);
        ) {
            if (resultSet.next()) {
                User user = new User(
                                resultSet.getLong("id"),
                                resultSet.getString("email"),
                                resultSet.getString("password"),
                                resultSet.getString("name"),
                                resultSet.getString("role"),
                                        resultSet.getString("phone"),
                                        resultSet.getString("height"),
                                        resultSet.getString("weight")
                );
                return user;
            }
*/
        /*String query= "SELECT weight"+" FROM Users "+
                " WHERE users.email='"+email+"' AND password='"+password+"'";*/
        double endPrice;
       return Double.parseDouble(null);
    }
}
